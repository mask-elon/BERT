---
description: "代码必须写极其详细的注释：文件/类/函数 docstring + 关键行内注释 + 论文公式与张量形状标注"
alwaysApply: true
---

# 代码必须写极其详细的注释（最高优先级约束）

本约束适用于本项目所有代码文件。写任何代码都必须配上**极其详细、清晰**的注释，
让读者无需运行即可理解意图、数据流与论文对应关系。

## 硬性要求

### 1. 文件顶部：模块 docstring
- 说明模块用途、所属阶段（如 Day 3/4/5）。
- 标注对应论文与章节/公式（如 BERT 3.1「Masked LM」、LoRA 4.1「Low-Rank Update」）。
- 列出设计原则与项目约束（如「禁止 import peft」「只训练 A/B，冻结 W_0」）。

### 2. 每个类 / 函数顶部：docstring
- 说明职责、输入/输出含义。
- 标注张量形状，使用统一记号（如 `[B, S, H]`、`[d, r]`）。
- 对应论文公式或算法步骤（写出公式，如 `h = W_0 x + (α/r) B A x`）。

### 3. 关键代码行内注释
- 解释**“为什么”**（意图、权衡、约束），而非仅复述**“做了什么”**。
- 数学/张量运算标注公式与维度变化（如 `# [B,A,S,d_k] -> [B,S,H]`）。
- 超参数注明取值依据（论文默认值 / 常见实践）。
- 显式标注踩坑点/易错点（如权重共享导致优化器参数重复、下载静默回退陷阱、
  GPU 静默回退陷阱等）。

## 正确 vs 错误示例

```python
# 好：解释意图 + 公式 + 形状
# 缩放点积注意力得分：Q K^T / sqrt(d_k)（Transformer 公式(1)），形状 [B,A,S,S]
scores = torch.matmul(q, k.transpose(-1, -2)) / math.sqrt(self.head_dim)

# 差：纯复述代码，无信息量（禁止）
scores = torch.matmul(q, k.transpose(-1, -2)) / math.sqrt(self.head_dim)  # 矩阵相乘再除
```

## 例外
- 不写纯废话注释（复述代码本身），注释必须传达代码无法表达的信息。
- 注释语言与项目保持一致（本项目用中文）。
