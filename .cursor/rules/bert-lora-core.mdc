---
description: BERT+LoRA 复现核心约束
globs: ["**/*.py"]
alwaysApply: true
---

# BERT + LoRA 从零复现项目约束

## 绝对禁止
- ❌ 禁止使用 `transformers` 库的 `BertModel`、`BertConfig`、`BertForMaskedLM` 等现成模型
- ❌ 禁止使用 `peft` 库的现成 LoRA 实现
- ❌ 禁止使用预训练权重（如 `bert-base-uncased` 的 checkpoint）
- ❌ 禁止过度工程化，保持最小可运行（MVP）

## 必须手写
- ✅ Token Embedding + Segment Embedding + Position Embedding（可学习或正弦）
- ✅ 双向 Multi-Head Self-Attention（手写 Q/K/V 投影、softmax、attention dropout）
- ✅ FFN（两个线性层 + GELU 激活）
- ✅ LayerNorm + 残差连接
- ✅ MLM 预训练头（输出层投影到 vocab_size）
- ✅ LoRA 适配器：ΔW = B×A，只训练 A、B，冻结原始权重

## 技术栈
- PyTorch 2.0+
- 数据集：WikiText-2（预训练）+ SST-2 子集（微调）
- 分词：简单 BPE 或 WordPiece（可用 `tokenizers` 库辅助，但编码逻辑自己写）
- 优化器：AdamW + warmup + 梯度裁剪
- 小规模配置：4层，256隐藏维度，4头注意力，512序列长度（CPU可跑）

## 代码规范
- 每个类/函数顶部注释标注对应论文章节或公式
- 变量名清晰：`d_model`, `num_heads`, `d_ff`, `num_layers`, `rank`, `alpha`
- 模块化：bert_model.py, lora.py, data.py, train_pretrain.py, finetune_full.py, finetune_lora.py, utils.py
- 所有脚本支持 `python xxx.py --config config.json` 运行